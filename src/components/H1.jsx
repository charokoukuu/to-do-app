export const H1 = ({ children }) => {
  return (
    <div
      style={{
        fontSize: "45px",
        fontWeight: "bold",
        margin: "auto",
        paddingTop: "50px",
        marginBottom: "28px",
      }}
    >
      {children}
    </div>
  );
};
